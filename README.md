# steam-deck-browser-game
It's only text and buttons, but I'm making it on  a steam deck with no external peripherals. This is just for backup purposes really.
Variables, if statements... and variables. Not professional.
[the KDE environment displays an error in VSC when trying to commit changes. It seems to work now, but still shows an error. Seems redundant. I will also be allowing certain commits from the browser to allow the copilot autofill because it just does that i guess]
